# Uncomment this line to define a global platform for your project
# platform :ios, '9.0'

target 'Check-in App' do
  # Comment this line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!
  pod 'Alamofire'
  pod 'SVProgressHUD'

  # Pods for Check-in App

  target 'Check-in AppTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'Check-in AppUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
